---
title: "SQD Developer Platform"
description: "Power your products with unparalleled real-time, blockchain indexing and Q&A capabilities."
mode: "wide"
---

export default function Home() {
  const [isClient, setIsClient] = React.useState(false);
  const [activeTab, setActiveTab] = React.useState('curl');

React.useEffect(() => {
setIsClient(true);

// Inject modern CSS
if (typeof document !== 'undefined') {
const style = document.createElement('style');
style.textContent = `
.hero-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 40px 24px;
}

.hero-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 60px;
}

.hero-title {
  font-size: 32px;
  font-weight: 700;
  color: var(--text-primary);
  margin: 0;
}

.hero-subtitle {
  font-size: 16px;
  color: var(--text-secondary);
  margin: 8px 0 0 0;
  max-width: 500px;
}

.quickstart-section {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: var(--background-secondary);
  border: 1px solid var(--border-primary);
  border-radius: 12px;
  padding: 24px;
  margin-bottom: 40px;
}

.quickstart-content {
  flex: 1;
}

.quickstart-title {
  font-size: 20px;
  font-weight: 600;
  color: var(--text-primary);
  margin: 0 0 8px 0;
  display: flex;
  align-items: center;
  gap: 12px;
}

.quickstart-description {
  font-size: 14px;
  color: var(--text-secondary);
  margin: 0;
}

.start-button {
  background: #6b7280;
  color: white;
  border: none;
  padding: 12px 24px;
  border-radius: 8px;
  font-weight: 600;
  font-size: 14px;
  cursor: pointer;
  transition: background 0.2s;
}

.start-button:hover {
  background: #4b5563;
}

.demo-section {
  background: #f5f5f0;
  border: 1px solid var(--border-primary);
  border-radius: 12px;
  padding: 32px;
  margin-bottom: 40px;
}

.demo-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24px;
}

.demo-title {
  font-size: 24px;
  font-weight: 700;
  color: var(--text-primary);
  margin: 0;
}

.demo-content {
  display: grid;
  grid-template-columns: 280px 1fr;
  gap: 24px;
}

.demo-sidebar {
  background: white;
  border: 1px solid var(--border-primary);
  border-radius: 8px;
  padding: 16px;
}

.demo-sidebar h3 {
  font-size: 16px;
  font-weight: 600;
  color: var(--text-primary);
  margin: 0 0 16px 0;
}

.demo-nav {
  list-style: none;
  padding: 0;
  margin: 0;
}

.demo-nav li {
  padding: 12px 16px;
  margin: 4px 0;
  border-radius: 6px;
  cursor: pointer;
  font-size: 14px;
  color: var(--text-secondary);
  transition: background 0.2s;
}

.demo-nav li:hover {
  background: var(--background-secondary);
}

.demo-nav li.active {
  background: var(--background-secondary);
  color: var(--text-primary);
  font-weight: 500;
}

.code-demo {
  background: white;
  border: 1px solid var(--border-primary);
  border-radius: 8px;
  overflow: hidden;
}

.code-tabs {
  display: flex;
  background: var(--background-secondary);
  border-bottom: 1px solid var(--border-primary);
}

.code-tab {
  padding: 12px 16px;
  font-size: 14px;
  color: var(--text-secondary);
  cursor: pointer;
  border: none;
  background: none;
  transition: all 0.2s;
}

.code-tab.active {
  color: var(--text-primary);
  background: white;
  border-bottom: 2px solid #3b82f6;
}

.code-content {
  padding: 24px;
  font-family: 'Monaco', 'Menlo', monospace;
  font-size: 14px;
  line-height: 1.6;
  color: var(--text-primary);
  background: white;
  min-height: 300px;
}

.code-line {
  margin: 4px 0;
}

.code-comment {
  color: #6b7280;
}

.code-string {
  color: #059669;
}

.code-keyword {
  color: #dc2626;
}

.code-property {
  color: #7c3aed;
}

.get-started-link {
  color: #6b7280;
  text-decoration: underline;
  font-size: 14px;
  margin-top: 16px;
  display: inline-block;
}

.features-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 24px;
  margin-top: 40px;
}

.feature-card {
  background: var(--background-secondary);
  border: 1px solid var(--border-primary);
  border-radius: 12px;
  padding: 24px;
}

.feature-card h3 {
  font-size: 18px;
  font-weight: 600;
  color: var(--text-primary);
  margin: 0 0 12px 0;
}

.feature-card p {
  font-size: 14px;
  color: var(--text-secondary);
  margin: 0;
  line-height: 1.5;
}

[data-theme="dark"] .demo-section {
  background: #1f2937;
}

[data-theme="dark"] .demo-sidebar,
[data-theme="dark"] .code-demo,
[data-theme="dark"] .code-content {
  background: #111827;
}

[data-theme="dark"] .code-tab.active {
  background: #111827;
}
`;
document.head.appendChild(style);
}
}, []);

const codeExamples = {
  curl: `curl https://api.sqd.dev/v1/query \\
  -H "Authorization: Bearer YOUR_API_KEY" \\
  -H "Content-Type: application/json" \\
  -d '{
    "query": "SELECT * FROM ethereum_blocks WHERE number > 18000000",
    "network": "ethereum"
  }'`,
  javascript: `import { SqdClient } from '@sqd/sdk';

const client = new SqdClient({
  apiKey: 'YOUR_API_KEY'
});

const blocks = await client.query({
  network: 'ethereum',
  query: 'SELECT * FROM blocks WHERE number > 18000000'
});`,
  python: `import sqd

client = sqd.Client(api_key="YOUR_API_KEY")

blocks = client.query(
  network="ethereum",
  query="SELECT * FROM blocks WHERE number > 18000000"
)`
};

return (
  <div className="hero-container">
    {/* Hero Header */}
    <div className="hero-header">
      <div>
        <h1 className="hero-title">sqd Developer Platform</h1>
        <p className="hero-subtitle">
          Power your products with unparalleled real-time, blockchain indexing and Q&A capabilities.
        </p>
      </div>
    </div>

    {/* Quickstart Guide */}
    <div className="quickstart-section">
      <div className="quickstart-content">
        <h2 className="quickstart-title">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
            <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
          </svg>
          Quickstart Guide
        </h2>
        <p className="quickstart-description">
          Getting started is simple and fast—make your first API call within minutes.
        </p>
      </div>
      <a href="/sdk/quickstart" className="start-button">
        Start Building
      </a>
    </div>

    {/* Try SQD Demo */}
    <div className="demo-section">
      <div className="demo-header">
        <h2 className="demo-title">Try SQD</h2>
      </div>
      
      <div className="demo-content">
        {/* Sidebar */}
        <div className="demo-sidebar">
          <h3>Get the Latest News</h3>
          <ul className="demo-nav">
            <li className="active">Domain Search</li>
            <li>Academic Search</li>
            <li>Structured Outputs</li>
          </ul>
          <a href="/sdk/how-to-start" className="get-started-link">
            Get started
          </a>
        </div>

        {/* Code Demo */}
        <div className="code-demo">
          <div className="code-tabs">
            <button 
              className={`code-tab ${activeTab === 'curl' ? 'active' : ''}`}
              onClick={() => setActiveTab('curl')}
            >
              curl
            </button>
            <button 
              className={`code-tab ${activeTab === 'javascript' ? 'active' : ''}`}
              onClick={() => setActiveTab('javascript')}
            >
              javascript
            </button>
            <button 
              className={`code-tab ${activeTab === 'python' ? 'active' : ''}`}
              onClick={() => setActiveTab('python')}
            >
              python
            </button>
            <div style={{ marginLeft: 'auto', padding: '12px 16px', display: 'flex', gap: '8px' }}>
              <button style={{ background: 'none', border: 'none', cursor: 'pointer' }}>📋</button>
              <button style={{ background: 'none', border: 'none', cursor: 'pointer' }}>↗️</button>
            </div>
          </div>
          
          <div className="code-content">
            <pre style={{ margin: 0, whiteSpace: 'pre-wrap' }}>
              {codeExamples[activeTab]}
            </pre>
            <div style={{ marginTop: '16px', fontSize: '12px', color: '#6b7280' }}>
              ... See all 12 lines
            </div>
          </div>
        </div>
      </div>
    </div>

    {/* Features Grid */}
    <div className="features-grid">
      <div className="feature-card">
        <h3>🚀 High Performance</h3>
        <p>
          Process millions of blockchain events with sub-100ms query latency. 
          Built for enterprise-scale applications.
        </p>
      </div>
      
      <div className="feature-card">
        <h3>🔧 Developer Experience</h3>
        <p>
          Hot reloading, TypeScript support, and instant feedback loops. 
          Ship faster with confidence.
        </p>
      </div>
      
      <div className="feature-card">
        <h3>🌐 Multi-Chain Support</h3>
        <p>
          Index data from 200+ blockchain networks including Ethereum, 
          Polygon, Arbitrum, and more.
        </p>
      </div>
      
      <div className="feature-card">
        <h3>🛡️ Enterprise Ready</h3>
        <p>
          99.9% uptime SLA, automatic scaling, monitoring, and alerting. 
          Built for mission-critical applications.
        </p>
      </div>
    </div>
  </div>
);

}





        const response = await fetch('https://v2.archive.subsquid.io/usage');
        if (!response.ok) throw new Error('Failed to fetch');

        const data = await response.json();

        const byVM = {};
        const vmCounts = {};

        Object.entries(data).forEach(([slug, network]) => {
          if (network && network.kind) {
            const vm = network.kind.toLowerCase();
            if (!byVM[vm]) {
              byVM[vm] = [];
              vmCounts[vm] = 0;
            }
            byVM[vm].push([slug, network]);
            vmCounts[vm]++;
          }
        });

        // Sort networks by usage
        Object.keys(byVM).forEach(vm => {
          byVM[vm].sort((a, b) => {
            const usageA = a[1]?.usage_bytes || 0;
            const usageB = b[1]?.usage_bytes || 0;
            return usageB - usageA;
          });
        });

        const total = Object.values(vmCounts).reduce((sum, count) => sum + count, 0);

        setNetworksData({ byVM, vmCounts, total });
        setLastUpdated(Date.now());
        setLoading(false);

      } catch (err) {
        console.error('Error loading networks:', err);
        setError('Failed to load networks');
        setLoading(false);
      }
    };

    loadNetworks();

    // Load GitHub data
    const loadGithubData = async () => {
      try {
        const response = await fetch('https://api.github.com/repos/subsquid/squid-sdk');
        const data = await response.json();
        setGithubData({
          stars: data.stargazers_count,
          url: data.html_url
        });
      } catch (err) {
        console.error('Error loading GitHub data:', err);
      }
    };

    loadGithubData();

}, [isClient]);

const vmLabels = {
evm: 'EVM',
substrate: 'Substrate',
solana: 'Solana',
fuel: 'Fuel',
tron: 'Tron',
starknet: 'Starknet'
};

return (

<main style={{ 
    minHeight: "100vh"
  }}>
  <div style={container}>
    
    {/* HERO SECTION */}
    <section style={heroSection} className="enterprise-hero">
      <div style={{ marginBottom: 24 }}>
        <div style={{
          display: "inline-flex",
          alignItems: "center",
          gap: 10,
          padding: "8px 20px",
          borderRadius: 24,
          color: "#ffffff",
          fontSize: 14,
          fontWeight: 600,
          marginBottom: 40,
          cursor: "pointer"
        }} className="stats-badge">
          <svg width="12" height="12" viewBox="0 0 12 12" fill="currentColor">
            <path d="M6 0L7.854 4.146L12 6L7.854 7.854L6 12L4.146 7.854L0 6L4.146 4.146L6 0Z"/>
          </svg>
          Trusted by 500+ Web3 teams
        </div>
      </div>
      
      <h1 style={{ 
        fontSize: 28, 
        fontWeight: 700, 
        margin: "0 0 12px", 
        lineHeight: 1.3, 
        fontFamily: "Inter, -apple-system, BlinkMacSystemFont, sans-serif",
        letterSpacing: "-0.02em"
      }} className="gradient-text">
        Enterprise blockchain<br />indexing infrastructure
      </h1>
      
      <p style={{ 
        color: "var(--sqd-text-muted)", 
        fontSize: 14, 
        margin: "0 auto 20px", 
        lineHeight: 1.4, 
        maxWidth: 420,
        fontFamily: "Inter, -apple-system, BlinkMacSystemFont, sans-serif",
        fontWeight: 400
      }}>
        Production-ready indexing for Web3 applications. Built for enterprise teams who demand performance, reliability, and exceptional developer experience.
      </p>
      
      <div style={{ 
        display: "flex", 
        gap: 12, 
        justifyContent: "center", 
        flexWrap: "wrap",
        marginBottom: 20
      }}>
        <a href="/sdk/quickstart" style={{...btnPrimary, padding: "12px 24px", fontSize: 14}} className="professional-button">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor">
            <path d="M8 5v14l11-7z"/>
          </svg>
          Start building
          <svg width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
            <path d="M8.22 2.97a.75.75 0 0 1 1.06 0l4.25 4.25a.75.75 0 0 1 0 1.06l-4.25 4.25a.75.75 0 0 1-1.06-1.06L11.44 8.5H2.75a.75.75 0 0 1 0-1.5h8.69L8.22 4.03a.75.75 0 0 1 0-1.06Z"/>
          </svg>
        </a>
        <a href="/why-subsquid" style={{...btn, padding: "12px 20px", fontSize: 14}} className="secondary-button">
          <svg width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
            <path d="M8 0a8 8 0 1 1 0 16A8 8 0 0 1 8 0ZM1.5 8a6.5 6.5 0 1 0 13 0 6.5 6.5 0 0 0-13 0Zm7-3.25v2.992l2.028.812a.75.75 0 0 1-.557 1.392l-2.5-1A.751.751 0 0 1 7 8.25v-3.5a.75.75 0 0 1 1.5 0Z"/>
          </svg>
          View demo
        </a>
        {githubData && (
          <a 
            href={githubData.url} 
            target="_blank" 
            rel="noopener noreferrer"
            className="github-component"
            style={{
              display: "inline-flex",
              alignItems: "center",
              gap: 8,
              padding: "12px 20px",
              fontSize: 14,
              textDecoration: "none"
            }}
          >
            <svg width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
              <path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"/>
            </svg>
            <span>SQD</span>
            <span className="github-star-count">
              ⭐ {githubData.stars.toLocaleString()}
            </span>
          </a>
        )}
      </div>
      
      {/* Stats */}
      <div style={{
        display: "flex",
        justifyContent: "center",
        gap: 48,
        flexWrap: "wrap",
        padding: "32px 0",
        borderTop: "1px solid var(--sqd-border-light)"
      }}>
        {[
          { label: "Networks supported", value: "200+" },
          { label: "Queries per second", value: "10K+" },
          { label: "Uptime SLA", value: "99.9%" }
        ].map((stat, index) => (
          <div key={stat.label} style={{ 
            textAlign: "center",
            animationDelay: `${index * 0.2}s`
          }}>
            <div style={{
              fontSize: 32,
              marginBottom: 8,
              fontFamily: "Inter, -apple-system, BlinkMacSystemFont, sans-serif",
              animationDelay: `${index * 0.2}s`
            }} className="stat-number">
              {stat.value}
            </div>
            <div style={{
              fontSize: 15,
              color: "var(--sqd-text-muted)",
              fontFamily: "Inter, -apple-system, BlinkMacSystemFont, sans-serif",
              fontWeight: 500
            }}>
              {stat.label}
            </div>
          </div>
        ))}
      </div>
    </section>

    {/* FEATURES SECTION */}
    <section style={{ marginBottom: 56, padding: "32px 24px", borderRadius: 12, border: "1px solid var(--sqd-border)", boxShadow: "0 8px 24px -4px rgba(0, 0, 0, 0.08)" }} className="features-background">
      <div style={{ textAlign: "center", marginBottom: 32 }}>
        <h2 style={{...sectionTitle, fontSize: 24, marginBottom: 12}}>Why enterprise teams choose SQD</h2>
        <p style={{...sectionSubtitle, fontSize: 14, maxWidth: 420}}>
          Built for teams that demand performance, reliability, and exceptional developer experience at scale.
        </p>
      </div>

      <div style={{
        display: "grid",
        gap: 16,
        gridTemplateColumns: "repeat(auto-fit, minmax(280px, 1fr))"
      }}>
        {[
          {
            icon: "⚡",
            title: "10x Performance",
            description: "Optimized data pipelines that outperform traditional indexing solutions. Process millions of events with sub-100ms query latency.",
            metrics: "< 100ms P95 latency"
          },
          {
            icon: "🔧",
            title: "Developer Experience",
            description: "Local development server with hot reloading, real-time type checking, and instant feedback loops. Ship faster with confidence.",
            metrics: "Hot reload in < 1s"
          },
          {
            icon: "🛡️",
            title: "Enterprise Reliability",
            description: "99.9% uptime SLA with automatic scaling, monitoring, and alerting. Built for mission-critical applications.",
            metrics: "99.9% uptime SLA"
          }
        ].map((feature, index) => (
          <div key={feature.title} style={{
            ...featureCard,
            animationDelay: `${index * 0.1}s`
          }} className="enterprise-card">
            <div style={{
              width: 56,
              height: 56,
              borderRadius: 16,
              background: "linear-gradient(135deg, var(--sqd-accent) 0%, var(--sqd-accent-light) 100%)",
              display: "flex",
              alignItems: "center",
              justifyContent: "center",
              fontSize: 24,
              marginBottom: 24,
              boxShadow: "0 8px 24px -4px rgba(59, 130, 246, 0.4)",
              animation: "float 3s ease-in-out infinite",
              animationDelay: `${index * 0.5}s`
            }}>
              {feature.icon}
            </div>

            <h3 style={{
              fontSize: 18,
              fontWeight: 700,
              margin: "0 0 10px",
              color: "var(--sqd-text-primary)",
              fontFamily: "Inter, -apple-system, BlinkMacSystemFont, sans-serif",
              letterSpacing: "-0.01em"
            }}>
              {feature.title}
            </h3>

            <p style={{
              color: "var(--sqd-text-secondary)",
              fontSize: 15,
              margin: "0 0 14px",
              lineHeight: 1.6,
              fontFamily: "Inter, -apple-system, BlinkMacSystemFont, sans-serif"
            }}>
              {feature.description}
            </p>

            <div style={{
              display: "inline-flex",
              alignItems: "center",
              gap: 8,
              padding: "6px 12px",
              background: "var(--sqd-surface-elevated)",
              border: "1px solid var(--sqd-border)",
              borderRadius: 8,
              fontSize: 14,
              fontWeight: 600,
              color: "var(--sqd-accent)",
              fontFamily: "Inter, -apple-system, BlinkMacSystemFont, sans-serif"
            }}>
              <svg width="12" height="12" viewBox="0 0 12 12" fill="currentColor">
                <path d="M6 0L7.854 4.146L12 6L7.854 7.854L6 12L4.146 7.854L0 6L4.146 4.146L6 0Z"/>
              </svg>
              {feature.metrics}
            </div>
          </div>
        ))}
      </div>
    </section>

    {/* CUSTOMER LOGOS SECTION */}
    <section style={{ marginBottom: 56, textAlign: "center", padding: "32px 24px", borderRadius: 12, border: "1px solid var(--sqd-border)", boxShadow: "0 8px 24px -4px rgba(0, 0, 0, 0.08)" }}>
      <div style={{
        color: "var(--sqd-text-muted)",
        fontSize: 12,
        fontWeight: 600,
        textTransform: "uppercase",
        letterSpacing: "0.1em",
        marginBottom: 20,
        fontFamily: "Inter, -apple-system, BlinkMacSystemFont, sans-serif"
      }}>
        Trusted by leading Web3 teams
      </div>

      <div style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        gap: 24,
        flexWrap: "wrap",
        opacity: 0.8
      }}>
        {[
          { name: "Ethereum", logo: "🔷" },
          { name: "Polygon", logo: "🟣" },
          { name: "Arbitrum", logo: "🔵" },
          { name: "Optimism", logo: "🔴" },
          { name: "Base", logo: "🔵" },
        ].map((customer, index) => (
          <div key={customer.name} style={{
            display: "flex",
            alignItems: "center",
            gap: 8,
            padding: "8px 16px",
            background: "var(--sqd-surface)",
            border: "1px solid var(--sqd-border)",
            borderRadius: 8,
            boxShadow: "0 8px 24px -4px rgba(0, 0, 0, 0.08)",
            fontSize: 24,
            fontWeight: 600,
            color: "var(--sqd-text-secondary)",
            fontFamily: "Inter, -apple-system, BlinkMacSystemFont, sans-serif",
            animationDelay: `${index * 0.1}s`
          }} className="customer-logo enterprise-card">
            <span style={{ fontSize: 16 }}>{customer.logo}</span>
            <span style={{ fontSize: 12 }}>{customer.name}</span>
          </div>
        ))}
      </div>
    </section>

    {/* COMPARISON SECTION */}
    <section style={{ marginBottom: 56, padding: "32px 24px", borderRadius: 12, border: "1px solid var(--sqd-border)", boxShadow: "0 8px 24px -4px rgba(0, 0, 0, 0.08)" }} className="comparison-table">
      <div style={{ textAlign: "center", marginBottom: 32 }}>
        <h2 style={{...sectionTitle, fontSize: 24, marginBottom: 12}}>How SQD compares</h2>
        <p style={{...sectionSubtitle, fontSize: 14, maxWidth: 420}}>
          See how we stack up against other blockchain indexing solutions in the market.
        </p>
      </div>

      <div style={comparisonTable}>
        <div style={{
          display: "grid",
          gridTemplateColumns: "240px repeat(5, 1fr)",
          gap: 0,
          fontSize: 14
        }}>
          {/* Header */}
          <div style={{
            padding: "20px 24px",
            background: "var(--sqd-surface-elevated)",
            borderRight: "1px solid var(--sqd-border)",
            fontWeight: 700,
            fontSize: 16,
            color: "var(--sqd-text-primary)",
            fontFamily: "Inter, -apple-system, BlinkMacSystemFont, sans-serif"
          }}>
            Features
          </div>
          {[
            { name: "SQD", highlight: true },
            { name: "The Graph", highlight: false },
            { name: "Goldsky", highlight: false },
            { name: "Ponder", highlight: false },
            { name: "Envio", highlight: false }
          ].map(({ name, highlight }) => (
            <div key={name} style={{
              padding: "20px 16px",
              background: highlight ? "linear-gradient(135deg, var(--sqd-accent) 0%, var(--sqd-accent-light) 100%)" : "var(--sqd-surface-elevated)",
              borderRight: "1px solid var(--sqd-border)",
              fontWeight: highlight ? 700 : 600,
              fontSize: 16,
              color: highlight ? "#ffffff" : "var(--sqd-text-secondary)",
              textAlign: "center",
              fontFamily: "Inter, -apple-system, BlinkMacSystemFont, sans-serif"
            }}>
              {name}
              {highlight && (
                <div style={{
                  display: "block",
                  marginTop: 8,
                  padding: "4px 12px",
                  background: "rgba(255, 255, 255, 0.2)",
                  color: "#ffffff",
                  fontSize: 12,
                  borderRadius: 6,
                  fontWeight: 600,
                  textTransform: "uppercase",
                  letterSpacing: "0.05em"
                }}>
                  YOU ARE HERE
                </div>
              )}
            </div>
          ))}

          {/* Comparison Rows */}
          {[
            {
              feature: "Indexing Performance",
              sqd: "10x faster",
              graph: "Baseline",
              goldsky: "2-3x faster",
              ponder: "Similar",
              envio: "2x faster"
            },
            {
              feature: "Supported Networks",
              sqd: "200+ chains",
              graph: "40+ chains",
              goldsky: "15+ chains",
              ponder: "10+ chains",
              envio: "20+ chains"
            },
            {
              feature: "Query Latency (P95)",
              sqd: "< 100ms",
              graph: "~500ms",
              goldsky: "~200ms",
              ponder: "~300ms",
              envio: "~250ms"
            },
            {
              feature: "Local Development",
              sqd: "Hot reload + TypeScript",
              graph: "Basic setup",
              goldsky: "Limited tooling",
              ponder: "Good DX",
              envio: "Basic setup"
            },
            {
              feature: "Enterprise SLA",
              sqd: "99.9% uptime",
              graph: "99.5% uptime",
              goldsky: "99.7% uptime",
              ponder: "Self-hosted",
              envio: "99.5% uptime"
            },
            {
              feature: "Architecture",
              sqd: "Open source",
              graph: "Decentralized",
              goldsky: "Proprietary",
              ponder: "Open source",
              envio: "Proprietary"
            }
          ].map((row, index) => (
            <>
              <div key={`${row.feature}-label`} style={{
                padding: "20px 24px",
                borderTop: index > 0 ? "1px solid var(--sqd-border)" : "none",
                borderRight: "1px solid var(--sqd-border)",
                fontWeight: 600,
                fontSize: 15,
                color: "var(--sqd-text-primary)",
                background: "var(--sqd-surface)",
                fontFamily: "Inter, -apple-system, BlinkMacSystemFont, sans-serif"
              }}>
                {row.feature}
              </div>
              {[row.sqd, row.graph, row.goldsky, row.ponder, row.envio].map((value, cellIndex) => {
                const isHighlight = cellIndex === 0;
                return (
                  <div key={`${row.feature}-${cellIndex}`} style={{
                    padding: "20px 16px",
                    borderTop: index > 0 ? "1px solid var(--sqd-border)" : "none",
                    borderRight: cellIndex < 4 ? "1px solid var(--sqd-border)" : "none",
                    fontSize: 14,
                    fontWeight: isHighlight ? 600 : 500,
                    color: isHighlight ? "var(--sqd-accent)" : "var(--sqd-text-secondary)",
                    textAlign: "center",
                    background: isHighlight ? "rgba(59, 130, 246, 0.05)" : "var(--sqd-surface)",
                    fontFamily: "Inter, -apple-system, BlinkMacSystemFont, sans-serif"
                  }}>
                    {value}
                  </div>
                );
              })}
            </>
          ))}
        </div>
      </div>
    </section>

    {/* SUPPORTED NETWORKS SECTION */}
    <section style={{ marginBottom: 56, padding: "32px 24px", borderRadius: 12, border: "1px solid var(--sqd-border)", boxShadow: "0 8px 24px -4px rgba(0, 0, 0, 0.08)" }}>
      <div style={{ textAlign: "center", marginBottom: 32 }}>
        <h2 style={{...sectionTitle, fontSize: 24, marginBottom: 12}}>Supported Networks</h2>
        <p style={{...sectionSubtitle, fontSize: 14, maxWidth: 420}}>
          {!isClient ? 'Loading network data...' :
           loading ? 'Loading network data...' :
           error ? error :
           networksData ? `Production-ready indexing across ${networksData.total} blockchain networks` :
           'Production-ready indexing across multiple blockchain networks'}
        </p>
        {isClient && lastUpdated && (
          <div style={{
            color: "var(--sqd-text-muted)",
            fontSize: 12,
            marginTop: 12,
            fontFamily: "Inter, -apple-system, BlinkMacSystemFont, sans-serif"
          }}>
            Last updated {formatTime(lastUpdated)}
          </div>
        )}
      </div>

      {/* VM Filters */}
      {isClient && networksData && networksData.vmCounts && (
        <div style={{
          display: "flex",
          gap: 12,
          marginBottom: 24,
          flexWrap: "wrap",
          justifyContent: "center"
        }}>
          {Object.entries(networksData.vmCounts)
            .sort(([a], [b]) => {
              if (a === 'evm') return -1;
              if (b === 'evm') return 1;
              return a.localeCompare(b);
            })
            .map(([vm, count]) => {
              const isActive = vm === activeVM;
              return (
                <button
                  key={vm}
                  onClick={() => setActiveVM(vm)}
                  style={{
                    padding: "10px 20px",
                    borderRadius: 8,
                    border: `1px solid ${isActive ? 'var(--sqd-accent)' : 'var(--sqd-border)'}`,
                    background: isActive ? 'rgba(59, 130, 246, 0.1)' : 'var(--sqd-surface-elevated)',
                    color: isActive ? 'var(--sqd-accent)' : 'var(--sqd-text-secondary)',
                    fontSize: 12,
                    fontWeight: 600,
                    fontFamily: "Inter, -apple-system, BlinkMacSystemFont, sans-serif",
                    cursor: "pointer",
                    transition: "all 0.3s cubic-bezier(0.4, 0, 0.2, 1)",
                    boxShadow: isActive ? "0 8px 25px -8px rgba(59, 130, 246, 0.3)" : "0 8px 24px -4px rgba(0, 0, 0, 0.08)"
                  }}
                  className="network-card"
                >
                  {vmLabels[vm] || vm.toUpperCase()} ({count})
                </button>
              );
            })}
        </div>
      )}

      {/* Networks Grid */}
      <div style={{
        display: "grid",
        gap: 16,
        gridTemplateColumns: "repeat(auto-fill, minmax(260px, 1fr))",
        marginBottom: 40
      }}>
        {!isClient || loading ? (
          <div style={{ gridColumn: "1 / -1", textAlign: "center", padding: 80 }}>
            <div style={{
              width: 48,
              height: 48,
              borderRadius: "50%",
              border: "4px solid var(--sqd-accent)",
              borderTopColor: "transparent",
              animation: "spin 1s linear infinite",
              margin: "0 auto 16px"
            }} />
            <div style={{
              color: "var(--sqd-text-muted)",
              fontSize: 16,
              fontFamily: "Inter, -apple-system, BlinkMacSystemFont, sans-serif"
            }}>
              Loading networks...
            </div>
          </div>
        ) : error ? (
          <div style={{
            gridColumn: "1 / -1",
            padding: 80,
            textAlign: "center",
            color: "var(--sqd-text-muted)",
            fontSize: 16,
            fontFamily: "Inter, -apple-system, BlinkMacSystemFont, sans-serif"
          }}>
            {error}
          </div>
        ) : networksData && networksData.byVM && networksData.byVM[activeVM] ? (
          networksData.byVM[activeVM].slice(0, 6).map(([slug, network]) => {
            if (!network || !network.name) return null;

            const isActive = network.last_updated_at && (Date.now() - new Date(network.last_updated_at).getTime() < 7200000);
            const usage = network.usage_bytes ? (network.usage_bytes / 1e9).toFixed(1) : '0';

            return (
              <a
                key={slug}
                href={`/network/${slug}`}
                style={{
                  ...networkCard,
                  padding: 20
                }}
                className="network-card"
              >
                <div style={{
                  width: 48,
                  height: 48,
                  background: "linear-gradient(135deg, var(--sqd-accent) 0%, var(--sqd-accent-light) 100%)",
                  borderRadius: 12,
                  display: "flex",
                  alignItems: "center",
                  justifyContent: "center",
                  fontWeight: 700,
                  fontSize: 16,
                  color: "#ffffff",
                  boxShadow: "0 4px 12px -4px rgba(59, 130, 246, 0.4)"
                }}>
                  {network.name.slice(0, 2).toUpperCase()}
                </div>
                <div style={{ flex: 1 }}>
                  <div style={{
                    fontWeight: 600,
                    fontSize: 16,
                    marginBottom: 4,
                    color: "var(--sqd-text-primary)",
                    fontFamily: "Inter, -apple-system, BlinkMacSystemFont, sans-serif"
                  }}>
                    {network.name}
                  </div>
                  <div style={{
                    color: "var(--sqd-text-muted)",
                    fontSize: 14,
                    fontFamily: "Inter, -apple-system, BlinkMacSystemFont, sans-serif"
                  }}>
                    {usage}GB indexed
                  </div>
                </div>
                <div style={{
                  width: 12,
                  height: 12,
                  borderRadius: "50%",
                  background: isActive ? "#10b981" : "#6b7280",
                  boxShadow: isActive ? "0 0 8px rgba(16, 185, 129, 0.4)" : "none"
                }} />
              </a>
            );
          }).filter(Boolean)
        ) : (
          <div style={{
            gridColumn: "1 / -1",
            padding: 60,
            textAlign: "center",
            color: "var(--sqd-text-muted)",
            fontSize: 16,
            fontFamily: "Inter, -apple-system, BlinkMacSystemFont, sans-serif"
          }}>
            No networks available
          </div>
        )}
      </div>

      {/* View All Button */}
      {isClient && networksData && networksData.byVM && networksData.byVM[activeVM] && networksData.byVM[activeVM].length > 6 && (
        <div style={{ textAlign: "center" }}>
          <a
            href="/network/overview"
            style={{
              ...btn,
              background: "rgba(59, 130, 246, 0.1)",
              border: "2px solid var(--sqd-accent)",
              color: "var(--sqd-accent)",
              fontWeight: 600
            }}
            className="professional-button"
          >
            View all {networksData.byVM[activeVM].length} {vmLabels[activeVM]} networks
            <svg width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
              <path d="M8.22 2.97a.75.75 0 0 1 1.06 0l4.25 4.25a.75.75 0 0 1 0 1.06l-4.25 4.25a.75.75 0 0 1-1.06-1.06L11.44 8.5H2.75a.75.75 0 0 1 0-1.5h8.69L8.22 4.03a.75.75 0 0 1 0-1.06Z"/>
            </svg>
          </a>
        </div>
      )}
    </section>

  </div>
</main>

);
}
