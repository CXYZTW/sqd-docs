```bash
# Set up environment variables
cp .env.example .env

# Configure database connection
DB_NAME=squid
DB_PORT=23798
GQL_PORT=4350

# For production deployments
SQD_TOKEN=your_deployment_token
```
